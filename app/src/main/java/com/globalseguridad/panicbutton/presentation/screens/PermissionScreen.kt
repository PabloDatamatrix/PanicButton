package com.globalseguridad.panicbutton.presentation.screens

import androidx.compose.runtime.Composable
import androidx.navigation.NavController

@Composable
fun PermissionScreen(navController: NavController) {
    // Implementar lógica para otorgar permisos
    // Una vez otorgados los permisos, navegar a la pantalla principal
//    navController.navigate(ScreensNavigation.HomeScreen.name) {
//        popUpTo(ScreensNavigation.PermissionScreen.name) { inclusive = true }
//    }
}
